# Optional: on Render add SPRING_PROFILES_ACTIVE=render so Gmail uses implicit SSL on 465 (avoids blocked STARTTLS on 587).
spring.config.activate.on-profile=render
spring.mail.port=${MAIL_PORT:465}
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=${MAIL_PORT:465}
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
