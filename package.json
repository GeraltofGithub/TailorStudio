{
  "name": "tailor-studio-monorepo",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "install:frontend": "npm --workspace frontend install",
    "build": "npm --workspace frontend run build"
  }
}

